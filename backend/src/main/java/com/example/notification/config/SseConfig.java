package com.example.notification.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SseConfig {
    // Basic SSE configuration
    // Spring Boot handles much of the setup automatically
    // once WebFlux is on the classpath.
    // Additional beans or configurations can be added here if needed.
}
